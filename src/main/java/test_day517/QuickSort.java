package test_day517;

/**
 * 快排
 */
public class QuickSort {
    public void quickSort(int[] arr, int start, int end) {
        if (start > end) {
            return;
        }
        int left, right, tmp, base;

        base = arr[0];
        left = arr[start];
        right = arr[end];

        /*while (start < end) {
            while (start <end && ){

            }

            while (){

            }



        }*/
    }
}
